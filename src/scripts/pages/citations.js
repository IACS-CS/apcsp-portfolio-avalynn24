import {renderMenu} from "../utils/renderMenu";

export function renderCitations() {
  /* html */
  return `
  <h1>Citations</h1>
  ${renderMenu()}
  <p>Initial template from my teacher Mr. Hinkle</p>
  <p>Used Copilot to help with code</p>
  <p>(later fill in libraries you use ect.)</p>
  `;
}